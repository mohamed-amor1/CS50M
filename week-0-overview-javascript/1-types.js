// Define a variable with a numerical value
const x = 42;

// Log the type of the variable 'x' to the console
console.log(typeof x);

// Log the type of the value 'null' to the console
console.log(typeof null); // The typeof operator returns 'object' for null, which is a known JavaScript quirk
